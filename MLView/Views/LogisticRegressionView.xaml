<UserControl x:Class="MLView.Views.LogisticRegressionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MLView.Views"
             xmlns:con="clr-namespace:MLView.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <con:TrainerControlPanel x:Name="TrainerControl" DockPanel.Dock="Top"/>
            <Button Content="Clear" Margin="10,0,0,0" Width="70" Click="Button_Click"/>
        </StackPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Width="230" Orientation="Vertical">
                <GroupBox Header="LinearRegression Config" Margin="5">
                    <con:LinearRegressionPanel x:Name="LinearRegConfig" Margin="5"/>
                </GroupBox>
                <GroupBox Header="Trainer Config" Margin="5">
                    <con:TrainerConfigPanel x:Name="TrainerConfig" Margin="5"/>
                </GroupBox>
                <GroupBox Header="Data Config" Margin="5">
                    <con:DataConfigPanel x:Name="DataConfig" Margin="5"/>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="LogText" Grid.RowSpan="2" BorderThickness="1" BorderBrush="LightGray" Margin="5" IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
            <Grid Background="Blue" Grid.Row="0" Grid.Column="1" />
            <Grid Background="Green" Grid.Row="0" Grid.Column="2"/>
            <Grid Background="Purple" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"/>
        </Grid>
    </DockPanel>
</UserControl>
